Put your 3 real PDFs in this folder with these EXACT filenames:

  linux-complete-cheatsheet.pdf
  docker-complete-cheatsheet.pdf
  kubernetes-complete-cheatsheet.pdf

The site's code already points to these paths (relative to index.html),
so once you drop the real files in here with these exact names, the
"Download PDF" buttons on the site will work immediately - no code
changes needed.

Delete this README before pushing to GitHub if you like (optional,
it won't break anything if you leave it).
